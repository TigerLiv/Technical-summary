```js
var subsets = function(nums) {
    let res = [[]];
    sub(0,[]);
    return res
    function sub(st,arr) {
        for(let i=st;i<nums.length;i++){
            arr.push(nums[i])
            res.push([...arr])
            sub(i+1,arr)
            arr.pop() // 回溯
        }
    }
};

```